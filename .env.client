# Vite — все переменные должны начинаться с VITE_
# DEV:  http://localhost:3000
# PROD: https://workvault.yourdomain.com  (или '' если фронт и бэк на одном домене)
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000